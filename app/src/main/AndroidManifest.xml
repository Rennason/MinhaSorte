<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.minhasorte">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_patrick"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_patrick"
        android:supportsRtl="true"
        android:theme="@style/Theme.MinhaSorte">
        <activity android:name=".SelecaoJogo"></activity>
        <activity android:name=".NumerosGeradosActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>